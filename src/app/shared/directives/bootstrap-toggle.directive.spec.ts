import { BootstrapToggleDirective } from './bootstrap-toggle.directive';

describe('BootstrapToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapToggleDirective();
    expect(directive).toBeTruthy();
  });
});
